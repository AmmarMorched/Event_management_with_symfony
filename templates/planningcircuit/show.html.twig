{% extends 'base.html.twig' %}

{% block title %}Planningcircuit{% endblock %}

{% block body %}
    <h1>Planningcircuit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nc</th>
                <td>{{ planningcircuit.nc }}</td>
            </tr>
            <tr>
                <th>Datedebut</th>
                <td>{{ planningcircuit.datedebut ? planningcircuit.datedebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ planningcircuit.capacite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planningcircuit_index') }}">back to list</a>

    <a href="{{ path('app_planningcircuit_edit', {'datedebut': planningcircuit.datedebut}) }}">edit</a>

    {{ include('planningcircuit/_delete_form.html.twig') }}
{% endblock %}
