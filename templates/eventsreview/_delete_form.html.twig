<form method="post" action="{{ path('app_eventsreview_delete', {'reviewId': eventsreview.reviewId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eventsreview.reviewId) }}">
    <button class="btn">Delete</button>
</form>
