<form method="post" action="{{ path('app_review_delete', {'reviewId': review.reviewId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ review.reviewId) }}">
    <button class="btn">Delete</button>
</form>
