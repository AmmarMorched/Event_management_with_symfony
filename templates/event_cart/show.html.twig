{% extends 'base.html.twig' %}

{% block title %}EventCart{% endblock %}

{% block body %}
    <h1>EventCart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CartId</th>
                <td>{{ event_cart.cartId }}</td>
            </tr>
            <tr>
                <th>TotalPrice</th>
                <td>{{ event_cart.totalPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_event_cart_index') }}">back to list</a>

    <a href="{{ path('app_event_cart_edit', {'cartId': event_cart.cartId}) }}">edit</a>

    {{ include('event_cart/_delete_form.html.twig') }}
{% endblock %}
