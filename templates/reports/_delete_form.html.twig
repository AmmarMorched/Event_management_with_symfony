<form method="post" action="{{ path('app_reports_delete', {'reportId': report.reportId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ report.reportId) }}">
    <button class="btn">Delete</button>
</form>
